=== 내가 작성한 리뷰 조회 ===

내가 작성한 리뷰 조회 기능입니다.

PathVariable로 alcoholId를 전달합니다.

- 유저의 ID가 존재하지 않으면 UserException이 발생합니다.

- Authorization 헤더에 액세스 토큰을 첨부하지 않으면`토큰이 존재하지 않습니다` 에러 메시지가 출력됩니다 (400)

- Authorization 헤더에 첨부한 토큰이 잘못 된 토큰이면 `잘못된 토큰입니다` 라는 에러 메시지가 출력됩니다 (401)

- Authorizatioin 헤더에 첨부한 토큰이 만료 된 토큰이면 `만료된 토큰입니다` 라는 에러 메시지가 출력됩니다 (403)

|====
|PathVariable |설명

|`alcoholId` | 위스키 ID

|====

API EndPoint

[source]
----
GET /api/v1/reviews/me/{alcoholId}
----

[discrete]
==== 요청 파라미터 ====

include::{snippets}/review/my-review-read/httpie-request.adoc[]
include::{snippets}/review/my-review-read/query-parameters.adoc[]

[discrete]
==== 응답 파라미터  ====

[discrete]
include::{snippets}/review/my-review-read/response-fields.adoc[]
include::{snippets}/review/my-review-read/http-response.adoc[]

[discrete]
===== _정렬 타입 (sortType)_ =====

[cols="1,1",options="header"]
|===
|  |
| POPULAR
| 인기순
| RATING
| 별점순
| BOTTLE_PRICE
| 병 가격 순
| GLASS_PRICE
| 잔 가격 순
|===

- 해당 값이 없을 경우 기본값은 POPULAR 입니다.
- 타입이 잘못 전달될 경우 예외가 반환됩니다.

include::../overview/sort-order.adoc[]
