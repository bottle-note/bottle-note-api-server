# Plan: 기존 알림함 발생처 확장

Status: Implemented (최신 원격 검증 결과는 PR checks 참조)

## Overview

bottle-note/workspace#383의 발생처 확장과 #373의 댓글·좋아요 활동 이벤트 공동화를 구현한다. 기존 notifications 테이블에 댓글·대댓글·좋아요·팔로우 알림을 저장하며 기존 문의 답변 동작을 유지한다.

## Scope

- 대댓글은 리뷰 작성자와 직접 부모 댓글 작성자에게 알린다. 행위자는 제외하고 동일 수신자는 답글 알림 한 건으로 합친다.
- 좋아요·팔로우는 활성 상태 전이에만 알림 대상이 된다. 반복·취소에는 생성하지 않고 재활성화해도 동일 관계의 최초 알림 한 건을 유지한다.
- 댓글·좋아요는 단일 활동 이벤트를 발행하고 History와 Notification이 각각 처리한다. History의 기존 기록 조건과 본문을 유지한다.
- 원본 rollback에는 부수효과를 만들지 않는다. 수신자별 알림 저장을 독립 트랜잭션으로 처리하고 기존 DB 고유 키로 동시 중복을 제거한다.
- 기존 목록·필터·HMAC cursor·미읽음·최초 읽음 계약을 유지한다. OPEN_REVIEW v1의 replyId를 보존하고 리뷰 상세 v2 빈 payload와 OPEN_USER v1을 추가한다.
- 사용자 최신 지시에 따라 새 테이블, user_push_configs 연동, 수신 설정 API, 푸시 발송은 제외한다.
- 베스트 리뷰 선정 변경과 알림 전달 구조는 폐기했다. 기존 Batch 코드는 그대로 두고 별도 PR에서 선정 방식을 교체한다.
- 수신 설정·보관·삭제·아카이브·운영 규모 성능 목표는 후속이다. #383 전체를 완료하거나 닫지 않는다.

## Impact Scope

- mono: review/like/user 활동 이벤트와 History·Notification 리스너, 멱등 저장, Action 계약.
- product-api: 알림 OpenAPI와 활동·저장 통합 테스트.
- test-support: 기존 InMemoryNotificationRepository에 멱등 저장 포트 구현.
- CI workflow는 기존 구성을 그대로 사용한다.
- 스키마, Batch, ProductApplication, Admin API와 설정 API에는 최종 변경을 남기지 않는다.

## Execution Mode

- mode: delegated
- scope: plan, implement, test, self-review, verify, commit, push, pr
- verify: 원격 commit·push 후 GitHub Actions에서만 수행한다. 로컬 Docker·Testcontainers·Gradle·컴파일·테스트·빌드·포맷 실행은 금지한다. 로컬에서는 코드·diff를 정적으로 검토한다.
- completion-evidence: 최종 HEAD SHA, Actions URL, 필수 job 결과, 실제 테스트 수와 실패 수, 미검증 항목.
- stop-conditions: 승인 가정 변경 시 문서와 범위 갱신, CI 실패를 세 차례 수정으로 해결하지 못함, merge·release·배포·운영 데이터 변경 등 범위 밖 작업.

## Tasks

- [x] 댓글·대댓글 수신자 정책과 단일 활동 이벤트 구현 및 정적 검토.
- [x] 좋아요·팔로우 활성 전이 알림 구현 및 정적 검토.
- [x] 기존 notifications 테이블의 원자적 중복 저장과 Action 확장 구현.
- [x] 단위·활동 통합·저장 동시성·OpenAPI 테스트 작성. 실행 결과는 미검증.
- [x] 베스트 리뷰·신규 테이블·수신 설정 구현 폐기 및 최종 범위 정리.
- [x] backend commit·push 후 GitHub Actions 전체 검증.
- [x] 원격 formatter 결과 반영, 구현 HEAD 검증, PR #748 생성 및 결과 기록. 완료 기록 커밋의 최신 검사 결과는 PR checks에서 확인한다.

- [x] 자체 리뷰에서 발견한 최초 좋아요 경쟁을 기존 리뷰 행 잠금과 최신 관계 조회로 수정하고 회귀 테스트를 추가한다. 원격 실행 결과는 PR checks에 기록한다.

## Verification

GitHub Actions의 unit_test, check_rule_test, integration_test, admin_integration_test와 build를 확인한다. 주요 시나리오는 직접 부모 수신자·동일 수신자·행위자 제외, 상태 전이별 최초 관계 알림, 원본 rollback, 수신자별 실패 격리, 동시 최초 insert와 재전달의 기존 읽음 상태 보존, 기존 문의 답변, 신규 Action JSON과 OpenAPI다.

기존 AFTER_COMMIT + Async 경로의 프로세스 장애 시 유실 가능성과 재시도 부재는 남는다. 이번 변경에서 Outbox나 새 전달 저장소를 추가하지 않는다.

## Progress Log

- 2026.09.05: Orca로 독립 구현 범위를 분담했고 모든 작업자는 실행 검증 없이 정적 검토 후 종료했다. 모든 담당 터미널을 release했다.
- 2026.09.05: schema PR #19는 999463b revert를 push한 뒤 닫았다. 실제 DB 적용·merge·배포는 없었다. backend gitlink는 작업 시작 시점의 7415667로 복원했다.
- 2026.09.05: 베스트 리뷰 선정·전달 변경을 제거하고 기존 Job 및 ProductApplication을 복원했다.
- 2026.09.05: 사용자가 기존 notifications 테이블 저장만으로 충분하다고 명확히 했다. 새 수신 설정 구현과 관련 테스트를 제거하고 기존 알림함 발생처 확장으로 범위를 정리했다.
- 2026.09.05: 첫 Actions run 33942910402는 unit 946, rule 66, 기존 Batch 106, Admin 통합 280을 통과했다. Product job은 mono 16 및 Product 367 중 OpenAPI 1개가 실패했다. 알림 저장·동시성·rollback·신규 Action 실제 JSON 시나리오는 통과했다.
- 2026.09.05: debug 절차로 실패 XML과 실제 생성 스펙을 확인했다. 테스트 resolve가 union을 첫 후보로 축소했고 문서용 sealed payload 상속이 순환 allOf와 겹치는 oneOf를 만들었다. 문서 모델의 상속을 제거하고 명시적 anyOf와 참조 보존 테스트로 수정했다. 원격 포맷 결과는 이번 변경 파일만 반영했다. native 저장 시각도 기존 JPA처럼 애플리케이션 LocalDateTime 기준으로 보존하고 관련 통합 검증을 보강했다.

- 2026.09.05: 두 번째 Actions run 33943220201에서 payload anyOf와 순환 참조 검증 및 모든 저장 테스트가 통과했다. OpenAPI 테스트의 마지막 문구 검사에서 OPEN_REVIEW v2 명칭 생략을 발견해 문서에 정확한 타입과 버전을 명시했다. formatter artifact에는 이번 PR 파일의 추가 변경이 없었다.

- 2026.09.05: 구현 HEAD 0e65636b04c9b17ae4cabcba75bb521b1d8b13d9의 Actions https://github.com/bottle-note/bottle-note-api-server/actions/runs/33943438240 에서 모든 job과 최종 build가 성공했다. XML 기준 unit 946, rule 66, 기존 Batch 106, Product job 통합 383, Admin 통합 280, 총 1781개이며 failures/errors/skipped 모두 0이다. 로컬 실행 검증은 0회다. PR은 https://github.com/bottle-note/bottle-note-api-server/pull/748 이며 merge·배포는 수행하지 않았다.

- 2026.09.05: 사용자 지적에 따라 작업 편의를 위해 추가했던 formatter artifact 단계를 제거했다. CI workflow를 작업 시작 시점과 동일하게 복원하고 기존 Actions로 검증한다.

- 2026.09.05: 사용자 수정 지시에 따라 최초 좋아요 경쟁 보완을 진행한다. 리뷰 행 잠금을 Facade로 획득하고 관계 조회·저장·커밋까지 유지한다. 기존 트랜잭션의 오래된 snapshot에서도 최신 관계를 읽도록 잠금 조회를 사용한다. 새 트랜잭션은 READ_COMMITTED로 불필요한 gap lock을 줄인다. 같은 리뷰의 요청이 직렬화되는 비용이 있으며 스키마·CI는 변경하지 않는다.

- 2026.09.05: 8개 최초 LIKE 동시 요청 및 과거 REPEATABLE_READ snapshot을 가진 경쟁 요청 회귀 테스트를 추가했다. 후속 취소·재활성화에도 같은 관계와 최초 알림을 유지하는지 확인한다. 정적 self-review와 diff 검토를 마쳤고 실행 검증은 커밋·푸시 후 기존 Actions에서 수행한다.
