services:
  dev-bottle-note:  # 서비스 이름이 정확해야 합니다.
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dev-bottle-note
    ports:
      - "15000:15000"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
