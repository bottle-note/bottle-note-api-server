name: deploy admin api on release publish

on:
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      tag:
        description: 'Admin release tag, for example admin/v1.2.3'
        required: true
        type: string

permissions:
  contents: read
  actions: read

concurrency:
  group: deploy-admin-production-${{ github.event.release.tag_name || inputs.tag }}
  cancel-in-progress: true

env:
  IMAGE_NAME: bottlenote-admin-api

jobs:
  verify-admin-release:
    name: verify admin release publish
    runs-on: ubuntu-latest
    outputs:
      release-tag: ${{ steps.release.outputs.release-tag }}
      version: ${{ steps.release.outputs.version }}
      image-tag: ${{ steps.release.outputs.image-tag }}
      tag-sha: ${{ steps.release.outputs.tag-sha }}
    steps:
      - name: checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: resolve admin release
        id: release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EVENT_NAME: ${{ github.event_name }}
          EVENT_TAG: ${{ github.event.release.tag_name || '' }}
          EVENT_DRAFT: ${{ github.event.release.draft || '' }}
          EVENT_PRERELEASE: ${{ github.event.release.prerelease || '' }}
          INPUT_TAG: ${{ inputs.tag || '' }}
        run: |
          set -euo pipefail
          release_tag="${EVENT_TAG:-$INPUT_TAG}"
          [[ "$release_tag" =~ ^admin/v[0-9]+\.[0-9]+\.[0-9]+$ ]] || { echo "::error::admin release tag required, got: $release_tag"; exit 1; }
          version="${release_tag#admin/v}"
          image_tag="admin_${version}"

          git fetch --no-tags origin '+refs/heads/main:refs/remotes/origin/main'
          git fetch --tags origin
          git rev-parse -q --verify "refs/tags/${release_tag}" >/dev/null || { echo "::error::release tag not found: $release_tag"; exit 1; }
          tag_sha="$(git rev-list -n 1 "$release_tag")"
          git merge-base --is-ancestor "$tag_sha" origin/main || { echo "::error::tag commit is not contained in origin/main: $tag_sha"; exit 1; }

          if [[ "$EVENT_NAME" == "release" ]]; then
            [[ "$EVENT_DRAFT" == "false" ]] || { echo "::error::release event must be published"; exit 1; }
            [[ "$EVENT_PRERELEASE" == "false" ]] || { echo "::error::production release must not be prerelease"; exit 1; }
          else
            releases_json="$(curl -fsSL \
              -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/${GITHUB_REPOSITORY}/releases?per_page=100")"
            RELEASES_JSON="$releases_json" RELEASE_TAG="$release_tag" python3 - <<'PY'
          import json, os, sys
          tag = os.environ['RELEASE_TAG']
          releases = json.loads(os.environ['RELEASES_JSON'])
          release = next((item for item in releases if item.get('tag_name') == tag), None)
          if release is None:
              print(f'::error::release not found for tag: {tag}', file=sys.stderr)
              sys.exit(1)
          if release.get('draft'):
              print('::error::release must be published', file=sys.stderr)
              sys.exit(1)
          if release.get('prerelease'):
              print('::error::production release must not be prerelease', file=sys.stderr)
              sys.exit(1)
          print(f"release_id={release.get('id')}")
          PY
          fi

          {
            echo "release-tag=$release_tag"
            echo "version=$version"
            echo "image-tag=$image_tag"
            echo "tag-sha=$tag_sha"
          } >> "$GITHUB_OUTPUT"
          echo "[admin-release-deploy] release_tag=$release_tag version=$version image_tag=$image_tag tag_sha=$tag_sha"

  build-and-push:
    name: build and push admin image
    needs: verify-admin-release
    runs-on: ubuntu-24.04-arm
    outputs:
      image-tag: ${{ needs.verify-admin-release.outputs.image-tag }}
      image-digest: ${{ steps.build.outputs.image-digest }}
    steps:
      - name: checkout release tag with submodules
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.verify-admin-release.outputs.release-tag }}
          submodules: true
          token: ${{ secrets.GIT_ACCESS_TOKEN }}

      - name: setup jdk
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: setup gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: build admin module
        run: |
          ./gradlew :bottlenote-admin-api:build -x test -x asciidoctor --build-cache --parallel

      - name: build and push to registry
        id: build
        uses: ./.github/actions/docker-build-push
        with:
          registry-url: ${{ secrets.REGISTRY_ADDRESS }}
          registry-username: ${{ secrets.REGISTRY_USERNAME }}
          registry-password: ${{ secrets.REGISTRY_PASSWORD }}
          image-name: ${{ env.IMAGE_NAME }}
          image-tag: ${{ needs.verify-admin-release.outputs.image-tag }}
          additional-tags: admin_latest
          dockerfile: Dockerfile-admin
          context: .
          platforms: linux/arm64
          cache-scope: admin-production
          image-title: "BottleNote Admin API"
          image-description: "BottleNote Admin API Server"
          image-vendor: "BottleNote"
          image-authors: "BottleNote Team"
          sign-image: 'true'
          cosign-key-path: 'git.environment-variables/storage/docker-registry/cosign.key'
          cosign-password: ${{ secrets.COSIGN_PASSWORD }}

  update-production:
    name: update admin production image
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: checkout code with submodules
        uses: actions/checkout@v4
        with:
          submodules: true
          token: ${{ secrets.GIT_ACCESS_TOKEN }}

      - name: setup kustomize
        uses: imranismail/setup-kustomize@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: update production image tag
        run: |
          IMAGE_TAG="${{ needs.build-and-push.outputs.image-tag }}"
          REGISTRY="${{ secrets.REGISTRY_ADDRESS }}"
          echo "Updating admin production image tag: $IMAGE_TAG"
          cd git.environment-variables
          git checkout main
          cd deploy/overlays/production
          kustomize edit set image bottlenote-admin-api=${REGISTRY}/${{ env.IMAGE_NAME }}:${IMAGE_TAG}
          echo "production kustomization.yaml updated"

      - name: commit and push with retry
        run: |
          cd git.environment-variables
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add deploy/overlays/production/kustomization.yaml
          if git diff --staged --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "chore(admin): update production image tag to ${{ needs.build-and-push.outputs.image-tag }}

          Updated by GitHub Actions
          Release: ${{ needs.verify-admin-release.outputs.release-tag }}
          Commit: ${{ needs.verify-admin-release.outputs.tag-sha }}
          Workflow: ${{ github.workflow }}"
          for i in {1..5}; do
            git pull --rebase origin main && git push origin main && break
            echo "Push failed, retry $i/5..."
            sleep $((i * 2))
          done
          echo "Admin production deployment triggered"
