# BottleNote Backend 릴리즈 지침

이 문서는 Hermes를 포함한 사람과 AI 운영자가 Product API와 Admin API를 릴리스할 때 보는 시작점이다. 세부 구현을 복제하기보다 **전체 흐름과 근거를 찾을 위치**를 안내한다.

마지막 코드·Actions 확인은 2026.09.05이며 Backend 원격 main `30639b1340d698d3543a0c92807d34fdd803aed1`을 기준으로 했다. 이번 확인은 현재 Pod·Registry 상태까지 검증한 결과가 아니다.

문서와 실제 동작이 다르면 `.github/workflows`와 참조 action의 현재 코드가 우선한다. 전체 서비스와 Kubernetes 운영은 [BottleNote 배포 운영 가이드](https://github.com/Whale0928/k8s-platform/blob/main/docs/BottleNote%20배포%20운영%20가이드.md)를 본다. 날짜가 붙은 증거는 당시 상태의 기록이며 이후 배포의 통과 근거로 재사용하지 않는다.

## 전체 흐름

```text
개발
main push → CI 성공 → 개발 이미지 발행
          → Image Updater → environment-variables/main → Argo CD → 개발 Pod

운영
release PR create 실행
  → releases/YYYY-MM-DD/N 생성
  → main에 빈 release commit 생성
  → main → releases/... PR 생성·실제 소스 CI 확인·병합
  → immutable 이미지 발행·서명 검증
  → 운영 latest 승격·재검증·Image Updater 인계
  → 일회용 release branch 삭제
  → Image Updater → environment-variables/main → Argo CD → 운영 Pod
```

GitHub Actions 성공과 Kubernetes 배포 완료는 다른 시점이다. Image Updater와 Argo CD가 비동기로 이어서 처리하므로 runtime까지 확인해야 한다.

## 개발 배포

일반 개발 배포는 별도 릴리스 작업이 없다.

1. 코드가 PR을 통해 `main`에 병합된다.
2. `ci pipeline`이 성공하면 `deploy development applications`가 그 main SHA를 사용한다.
3. Product와 Admin immutable 이미지를 발행·서명 검증한 뒤 `*_latest_development`를 갱신하고 재검증한다.
4. Image Updater가 30초 주기로 digest를 Git write-back한다.
5. Argo CD가 `bottlenote-development`에 반영한다.

수동 배포가 필요하면 다른 ref를 실수로 고르지 않도록 main을 명시한다.

```bash
gh workflow run deploy_development_applications.yml --ref main
```

## 운영 릴리스

### 1. 대상 결정

현재 `origin/main`에 배포할 코드가 있고 CI가 성공했는지 확인한다. 같은 날짜에 이미 사용한 branch와 PR을 보고 다음 release key `YYYY-MM-DD/N`을 고른다.

Backend mode는 다음 세 가지다.

| mode | 대상 |
| --- | --- |
| `both` | Product와 Admin, 기본값 |
| `product` | Product만 |
| `admin` | Admin만 |

운영 workflow의 Gradle build는 `-x test`이므로 테스트 근거는 main CI다.

### 2. Release PR 생성

`release PR create`를 main 기준으로 실행한다. `releases/**` 브랜치는 수동으로 만들지 않고 이 workflow로만 생성한다. 생성기가 보장하는 base·source·marker 계약을 수동 ref 생성으로 우회하지 않는다.

```bash
gh workflow run release_pr_pilot_create.yml \
  --ref main \
  -f release_date=YYYY-MM-DD \
  -f sequence=N \
  -f deployment_mode=both
```

workflow가 실행 직전 main을 `releases/YYYY-MM-DD/N`에 고정하고, main에는 `release(<mode>): YYYY-MM-DD/N` 형식의 빈 commit을 추가한다. 그다음 **head `main` → base `releases/YYYY-MM-DD/N`** 방향으로 PR을 연다.

일반 코드 PR과 방향이 반대다. release branch에서 main으로 PR을 열면 안 된다. PR 본문의 source SHA는 새 빈 commit이며, 그 부모가 실행 직전 main이다. 두 commit의 file tree는 같다.

### 3. 확인하고 merge

생성된 PR에서는 다음을 확인한다.

- base가 `releases/YYYY-MM-DD/N`, head가 `main`인가
- changed files, additions, deletions가 모두 0인가
- source SHA, release key, mode가 요청한 값과 같은가
- source의 부모 SHA와 tree가 사전에 CI를 확인한 소스와 일치하는가

생성기는 실행 중 최신 원격 main을 다시 읽는다. 준비 이후 main이 전진했다면 사전에 확인한 CI는 실제 릴리스 소스의 근거가 아닐 수 있다. 생성된 source SHA의 CI를 확인하거나, 빈 commit임과 동일 tree를 확인한 부모 SHA의 CI 성공을 연결한다. 다른 소스의 과거 성공으로 대체하지 않는다. 현재 일반 Release PR의 생성·병합 gate는 CI 성공을 자동 조회하지 않으므로 운영자가 병합 전에 확인해야 한다.

현재 workflow에는 별도 reviewer approval 검사가 없다. **merge 권한자가 merge하는 행위 자체가 운영 배포 승인**이다. 저장소의 현재 보호 규칙은 별도로 확인한다. GitHub App도 필요한 PR·contents 권한과 ruleset 통과 권한이 있으면 같은 방식으로 merge할 수 있다.

```bash
gh pr merge <PR 번호> --merge
```

상태 확인 요청만으로 workflow dispatch나 merge를 실행하지 않는다. 사용자가 운영 릴리스를 명시적으로 요청했을 때만 실행한다.

### 4. merge 뒤 자동 처리

`release PR deployment`는 `pull_request_target`으로 PR 방향, source, key, mode, zero diff와 commit tree를 다시 검증한다. 각 이미지의 공개 순서는 다음과 같다.

```text
immutable 태그 push → digest 확인 → Cosign 서명 → 서명 검증
  → 같은 digest로 *_latest_production 승격 → digest·서명 재검증 → handoff
```

승격 전 실패하면 해당 이미지의 기존 production latest는 유지된다. 승격 후 실패는 이미 채널이 바뀌었을 수 있으므로 실제 digest를 확인한다. 현재 action에는 별도 취약점 scan이나 quarantine 저장소가 없다. 운영에서는 cache export를 끄며, 개발의 cache export는 승격 이후 실패 허용 단계로 실행한다.

`both`는 Product와 Admin을 독립 job에서 처리한다. 한쪽 승격 후 다른 쪽이 실패하면 전체 run이 실패해도 한쪽 채널은 이미 갱신됐을 수 있다. 두 API의 동시 승격이나 자동 rollback을 보장하지 않으므로 실패 시 각 이미지의 이전·현재 digest와 GitOps 반영 여부를 따로 확인한다.

애플리케이션 코드는 고정 `source_sha`로 checkout하지만, 공용 Docker action은 실행 중 원격 main에서 갱신한다. 애플리케이션 소스와 빌드 action의 출처를 구분하고 실행 로그에 남은 근거를 확인한다.

예를 들어 `2026-08-29/3`의 `both`는 `product_2026.08.29.3`, `admin_2026.08.29.3`과 각 production latest를 만든다. Image Updater는 latest의 새 digest를 `environment-variables/main`에 커밋하고 Argo CD가 이를 동기화한다.

일회용 release branch는 이미지 발행과 handoff가 성공하면 자동 삭제된다. 실제 Pod rollout보다 먼저 삭제될 수 있으므로 branch 삭제만으로 완료를 판단하지 않는다.

빈 release commit도 main push이므로 개발 CI와 개발 배포가 함께 실행된다. 이는 현재 의도된 동작이다.

## 완료 확인

- [ ] Release PR의 source, mode와 zero diff가 맞다.
- [ ] 생성된 source 또는 동일 tree인 부모 SHA의 CI 성공을 확인했다.
- [ ] 선택한 이미지별 build, immutable push, 서명 검증, latest 승격·재검증과 handoff가 성공했다.
- [ ] immutable 태그와 production latest의 최상위 OCI digest가 같다.
- [ ] `environment-variables/main`에 Image Updater 커밋이 생겼다.
- [ ] `bottlenote-production`이 기대 revision에서 `Synced`, `Healthy`, `Succeeded`다.
- [ ] Deployment와 Pod가 새 digest로 Ready이며 restart가 비정상적으로 증가하지 않았다.
- [ ] readiness가 `UP`이고 필요한 기능 smoke check가 성공했다.
- [ ] 일회용 release branch가 남아 있지 않다.

30초는 Image Updater의 조회 주기다. 일부 workflow summary의 과거 `5 minute interval` 문구보다 플랫폼의 실제 설정을 우선한다. Registry 조회, Git push, Argo refresh와 애플리케이션 기동 시간은 별도로 걸린다. 잠시 `OutOfSync` 또는 `Progressing`인 것은 정상적인 중간 상태일 수 있다.

## 다른 서비스와 핫픽스

Backend의 일반 Release PR 규칙을 모든 서비스에 그대로 적용하지 않는다. 2026.09.05 원격 확인 기준은 다음과 같다.

| 서비스 | 일반 릴리즈와 예외 |
| --- | --- |
| Backend | Product/Admin은 위 Release PR 경로다. 핫픽스 전용 자동화는 없다. Batch는 아래 별도 흐름을 따른다. |
| Frontend | 일반 Release PR을 사용한다. 핫픽스 전용 자동화는 없으며 개발 배포는 main push에서 CI와 병렬로 시작한다. |
| Admin Dashboard | 일반 Release PR과 전용 핫픽스 흐름이 분리되어 있다. 개발은 CI 성공 후 시작하지만 현재 CI는 build 중심이며 테스트 실행을 뜻하지 않는다. |
| 모바일 앱 | Android의 release 라벨이 붙은 PR 병합 또는 dispatch로 AAB를 빌드하고 Play Store internal·draft에 올린다. 웹 서비스의 Image Updater 경로가 아니다. |

일반 Release PR에는 세 웹 서비스 모두 CI 성공을 자동 조회하는 gate가 없다. Dashboard 핫픽스만 후보 SHA의 `ci` 성공을 검사한다. Frontend와 Dashboard 이미지의 `GIT_COMMIT`·OCI revision은 이제 전달받은 배포 `source-sha`를 기록하며, 과거의 이벤트 SHA 설명을 적용하지 않는다.

Dashboard 핫픽스는 `release_pr_create.yml`의 `release_type=hotfix`로 `hotfixes/YYYY-MM-DD/N`을 만들고, 수정 브랜치 `hotfix/<name>`에서 해당 base로 PR을 연다. 일반 릴리즈와 달리 실제 코드 변경이 있어야 하며, 후보 CI와 merge tree를 검증한 **merge SHA**가 배포 대상이다. base는 마지막으로 필수 job들이 성공한 일반·핫픽스 run의 소스에서 고르므로 실제 운영 Pod의 revision과 같다고 단정하지 않는다. 자동 main 역병합은 없으며 수정 내용의 main 반영은 별도 PR로 진행한다.

새 Dashboard 핫픽스 workflow는 위 확인 시점에 실행 이력이 0건이었다. 구현된 계약과 운영 성공 실적을 구분한다. 정확한 입력과 검증 조건은 [핫픽스 생성기](https://github.com/bottle-note/admin-dashboard/blob/1595fcc7fee3dfcb34757cf4a3b3ae2de66a5685/.github/workflows/release_pr_create.yml), [핫픽스 병합 gate](https://github.com/bottle-note/admin-dashboard/blob/1595fcc7fee3dfcb34757cf4a3b3ae2de66a5685/.github/workflows/hotfix_pr_merged.yml)를 따른다.

## 근거를 찾을 위치

Backend 저장소:

- PR 생성: `.github/workflows/release_pr_pilot_create.yml`
- merge 검증과 branch 삭제: `.github/workflows/release_pr_pilot_merged.yml`
- 운영 build와 image 발행: `.github/workflows/deploy_release_applications.yml`
- main CI: `.github/workflows/ci_pipeline.yml`
- 개발 배포: `.github/workflows/deploy_development_applications.yml`
- 공용 image build·서명: `.github/actions/docker-build-push/action.yml`

GitOps와 플랫폼:

- 환경 overlay: `git.environment-variables/deploy/overlays/{development,production}`
- Argo Application: `git.environment-variables/deploy/argocd/bottlenote/applications`
- Image Updater: `/Users/hgkim/workspace/k8s-platform/platform/image-manager`
- 상세 운영 가이드: `k8s-platform/docs/BottleNote 배포 운영 가이드.md` (상단 원격 링크)

이 저장소의 `git.environment-variables`는 source commit에 고정된 submodule snapshot이다. 실제 Argo CD와 Image Updater는 `bottle-note/environment-variables`의 `main`을 계속 따라간다. digest write-back이 생길 때마다 이 저장소의 submodule pointer를 갱신할 필요는 없다.

문제가 생기면 순서를 거슬러 올라간다: Pod와 Argo → GitOps commit → Image Updater log → Registry digest → Actions와 Release PR. live Deployment를 직접 수정하거나 `kubectl rollout undo`로 GitOps를 우회하지 않는다.

표준 운영 릴리스는 Release PR 경로다. 호환 목적으로 남은 `backend/vX.Y.Z` GitHub Release published 경로는 Image Updater 흐름과 충돌할 수 있으므로 사용하지 않는다.

## Batch는 별도 흐름

Batch는 Release PR과 Image Updater 대상이 아니다. `.github/workflows/deploy_batch.yml`을 정확한 `X.Y.Z` 버전으로 수동 실행한다. production은 main에서만 허용되고 GitHub Environment를 사용한다. 2026.09.05 조회에서는 해당 Environment의 `protection_rules`가 비어 있어 별도 승인자 gate는 없었다. Environment 사용 자체를 승인 대기로 해석하지 말고 실행 시점의 보호 설정을 확인한다. 운영 배포에 대한 사용자 허가는 여전히 필요하다.

```bash
gh workflow run deploy_batch.yml \
  --ref main \
  -f environment=production \
  -f version=X.Y.Z
```

Batch workflow는 `batch_X.Y.Z` 이미지를 발행한 뒤 대상 overlay를 직접 Git write-back한다. 이미 사용한 태그는 덮어쓰지 않는다. 이미지 발행 후 GitOps가 실패했다면 버전은 이미 소비되었을 수 있으므로 실패 단계와 태그 상태를 확인하고 새 버전을 사용한다. 8월 29일 성공 run은 9월 4일 공용 action 개선 전의 결과이므로 최신 Batch 경로의 실행 증거로 쓰지 않는다.
